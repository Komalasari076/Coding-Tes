function menjumlahkanDataArray(arr) {

    let arr = [1, 3, 5, 6];
    let sum = 0;

    for (i = 0; i < arr.length; i++) {
        sum += arr(i);
        return sum;
    }
}

console.log(menjumlahkanDataArray(arr));